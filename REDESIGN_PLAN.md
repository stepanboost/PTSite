# ПЛАН РЕДИЗАЙНА ЛЕНДИНГА PROVOLTA & TEAM SERVICE
## Senior Product Designer + Conversion Copywriter + Front-End Lead

---

## A) НОВАЯ СТРУКТУРА ЛЕНДИНГА (порядок блоков)

### Итоговый флоу (9 блоков вместо 13):

1. **Hero** *(оставить, усилить)*
2. **ProofBar** *(новый — цифры + trust-badges)*
3. **Services** *(оставить, но UI переделать на split-screen)*
4. **Cases** *(переместить в топ-3! Главный trust-блок)*
5. **InStock** *(оставить, усилить как предложение)*
6. **HowWeWork** *(объединить Lifecycle + Process в timeline)*
7. **TrustBlock** *(объединить WhyChoose + Guarantee в 1 мощный блок)*
8. **FAQ** *(оставить, accordion)*
9. **FinalCTA** *(усилить, добавить срочность)*
10. **Contacts** *(оставить)*
11. **Footer** *(минималистичный)*

### ❌ Что УДАЛИТЬ/ОБЪЕДИНИТЬ:

| Старый блок | Действие | Почему |
|-------------|----------|--------|
| **OneCenter** | Удалить | Дублирует hero-месседж. Интегрировать в ProofBar (1 строка) |
| **Lifecycle** | Объединить | Объединить с Process → HowWeWork (timeline без карточек) |
| **WhyChoose** | Объединить | Объединить с Guarantee → TrustBlock (1 мощная секция) |
| **ContactForm** | Удалить | Слишком рано в воронке. Достаточно 2 CTA + липкая кнопка |
| **Process** | Объединить | См. выше (с Lifecycle) |

### 🎯 2 ГЛАВНЫЕ CTA:

**CTA #1: "Рассчитать стоимость под ключ"** (quiz)
- Hero (основная кнопка)
- Cases (после кейсов)
- Sticky button (всегда видна)

**CTA #2: "Показать авто в наличии"** (якорь на InStock)
- Hero (вторая кнопка)
- TrustBlock (после доверия)

---

## B) ANTI-CARD СТРАТЕГИЯ (как сделать секции разными)

### 1. **Hero** — Fullscreen immersive
**UI-паттерн:** Hero с параллакс-эффектом + floating elements
- Без карточек
- Большое фото/видео на фоне (китайский авто-завод или готовый Zeekr в московском сервисе)
- Логотип + заголовок + 2 кнопки
- Плавающие trust-badges (Shield, Clock, Star)

**Код:**
```tsx
// Hero.tsx — уже есть, но убрать карточки с features внизу
// Оставить только заголовок + 2 CTA + floating badges
```

---

### 2. **ProofBar** — Horizontal stats strip (новый)
**UI-паттерн:** Full-width горизонтальная полоса с цифрами (no cards, no shadows)
- Фон: `bg-neutral-900` (тёмный контраст после светлого hero)
- Белый текст, акцентные цифры красным
- 4 колонки: Авто импортировано | Лет на рынке | Выполнено за 2025 | Повторных клиентов

**Структура:**
```tsx
// components/ProofBar.tsx (создать)
<section className="bg-neutral-900 py-8 border-y border-neutral-800">
  <div className="container-custom">
    <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
      {stats.map(stat => (
        <div className="text-center">
          <div className="text-4xl md:text-5xl font-bold text-primary-500 mb-1">
            {stat.value}
          </div>
          <div className="text-sm text-neutral-400">{stat.label}</div>
        </div>
      ))}
    </div>
  </div>
</section>
```

**Контент:**
```js
const stats = [
  { value: '420+', label: 'Авто импортировано из Китая' },
  { value: '7 лет', label: 'Работаем с автоимпортом' },
  { value: '180+', label: 'Выполнено в 2025 году' },
  { value: '68%', label: 'Клиентов возвращаются' },
]
```

---

### 3. **Services** — Split-screen layout
**UI-паттерн:** Текст слева, интерактивная карта справа (NO CARDS!)
- Левая колонка: список услуг (текст + иконки, hover → детали)
- Правая колонка: большое изображение меняется при hover
- Mobile: вертикальный stack с большими тапаемыми зонами

**Структура:**
```tsx
// Services.tsx — переделать
<section className="section-padding bg-neutral-100">
  <div className="container-custom">
    <h2>Все услуги под ключ</h2>
    <div className="grid md:grid-cols-2 gap-12 mt-12">
      
      {/* Левая колонка - список */}
      <div className="space-y-4">
        {services.map(service => (
          <motion.button
            onClick={() => setActive(service.id)}
            className={`w-full text-left p-6 rounded-xl transition-all ${
              active === service.id 
                ? 'bg-neutral-900 text-white' 
                : 'bg-white hover:bg-neutral-50'
            }`}
          >
            <div className="flex items-start gap-4">
              <Icon className="w-8 h-8 flex-shrink-0" />
              <div>
                <h3 className="font-bold text-lg mb-1">{service.title}</h3>
                <p className="text-sm opacity-70">{service.short}</p>
              </div>
            </div>
          </motion.button>
        ))}
      </div>

      {/* Правая колонка - визуал */}
      <motion.div 
        className="relative aspect-[4/3] rounded-2xl overflow-hidden"
        initial={false}
        animate={{ opacity: 1 }}
      >
        <Image 
          src={services.find(s => s.id === active)?.image} 
          fill 
          className="object-cover"
        />
        <div className="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent" />
        <div className="absolute bottom-8 left-8 right-8 text-white">
          <p className="text-lg">{services.find(s => s.id === active)?.details}</p>
        </div>
      </motion.div>

    </div>
  </div>
</section>
```

---

### 4. **Cases** — Gallery slider + filters (★ ГЛАВНЫЙ БЛОК)
**UI-паттерн:** Horizontal slider с крупными карточками-кейсами
- Карточки можно, НО большие (не как везде)
- Фильтры сверху (табы, не карточки)
- Каждая карточка: фото слева (галерея) + детали справа
- Before/After для защитных пленок/русификации

**Структура:**
```tsx
// Cases.tsx — переделать полностью
<section className="section-padding bg-white">
  <div className="container-custom">
    <h2>Наши кейсы — реальные авто реальных клиентов</h2>
    
    {/* Фильтры */}
    <div className="flex flex-wrap gap-3 my-8">
      {filters.map(filter => (
        <button className={`px-6 py-3 rounded-full border-2 transition-all ${
          activeFilter === filter.id
            ? 'bg-neutral-900 text-white border-neutral-900'
            : 'bg-white text-neutral-700 border-neutral-300 hover:border-neutral-900'
        }`}>
          {filter.label}
        </button>
      ))}
    </div>

    {/* Slider */}
    <div className="relative">
      <div className="overflow-x-auto snap-x snap-mandatory scrollbar-hide pb-8">
        <div className="flex gap-6" style={{ width: 'max-content' }}>
          {cases.map(caseItem => (
            <motion.div 
              className="w-[90vw] md:w-[600px] snap-start"
              whileHover={{ y: -8 }}
            >
              {/* Левая часть - галерея фото */}
              <div className="bg-neutral-100 rounded-2xl overflow-hidden mb-4">
                <div className="aspect-[16/10] relative">
                  <Image src={caseItem.images[currentImg]} fill className="object-cover" />
                  {/* Навигация по фото */}
                </div>
              </div>

              {/* Правая часть - детали */}
              <div className="space-y-4">
                <div className="flex items-center gap-3">
                  <span className="px-3 py-1 rounded-full bg-primary-500/10 text-primary-500 text-xs font-bold">
                    {caseItem.category}
                  </span>
                  <span className="text-sm text-neutral-500">{caseItem.duration}</span>
                </div>

                <h3 className="text-2xl font-bold">{caseItem.title}</h3>
                
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <div className="text-sm text-neutral-500 mb-1">Проблема</div>
                    <div className="font-medium">{caseItem.problem}</div>
                  </div>
                  <div>
                    <div className="text-sm text-neutral-500 mb-1">Результат</div>
                    <div className="font-medium text-primary-500">{caseItem.result}</div>
                  </div>
                </div>

                <div>
                  <div className="text-sm text-neutral-500 mb-2">Что сделали:</div>
                  <div className="flex flex-wrap gap-2">
                    {caseItem.services.map(service => (
                      <span className="px-3 py-1 rounded-lg bg-white border border-neutral-200 text-sm">
                        {service}
                      </span>
                    ))}
                  </div>
                </div>

                {caseItem.price && (
                  <div className="pt-4 border-t border-neutral-200">
                    <div className="text-sm text-neutral-500">Стоимость под ключ</div>
                    <div className="text-3xl font-bold mt-1">{caseItem.price}</div>
                  </div>
                )}
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </div>

    {/* CTA */}
    <div className="text-center mt-12">
      <button className="btn-primary">
        Рассчитать стоимость моего кейса
      </button>
    </div>
  </div>
</section>
```

**Фильтры:**
```js
const filters = [
  { id: 'all', label: 'Все кейсы' },
  { id: 'import', label: 'Импорт из Китая' },
  { id: 'russian', label: 'Русификация' },
  { id: 'tuning', label: 'Улучшения' },
  { id: 'service', label: 'Сервис и ТО' },
  { id: 'warranty', label: 'Гарантия' },
  { id: 'resale', label: 'Продажа авто' },
]
```

**Шаблон кейса (6–8 карточек):**
```js
const cases = [
  {
    id: 1,
    category: 'Импорт + под ключ',
    title: 'Zeekr 9X MAX из Китая',
    duration: '14 дней',
    problem: 'Клиент хотел Zeekr 9X MAX, в РФ не продаётся',
    result: '12,5 млн под ключ с полной подготовкой',
    services: ['Импорт', 'PPF', 'Шумоизоляция', 'Антикор', 'ТО 0', 'Русификация', 'КАСКО+ОСАГО', 'Гарантия'],
    price: '12 500 000 ₽',
    images: ['/image/case/zeeker/zeekr-01.jpeg', ...],
    proof: 'Акт приёмки + фото + видео',
  },
  {
    id: 2,
    category: 'Русификация',
    title: 'Mercedes-Benz EQS — полная русификация',
    duration: '2 дня',
    problem: 'Интерфейс на английском, голосовой помощник не работал',
    result: 'Полностью русифицированный интерфейс + настройка Яндекс.Авто',
    services: ['Русификация мультимедиа', 'Интеграция Яндекс', 'Настройка приборки'],
    price: 'от 85 000 ₽',
    images: ['[PLACEHOLDER — скриншот до/после интерфейса]'],
    proof: 'Видео демонстрация',
  },
  {
    id: 3,
    category: 'Защита + улучшения',
    title: 'Hongqi E-HS9 — премиум защита',
    duration: '5 дней',
    problem: 'Новое авто требовало максимальной защиты ЛКП и антикора',
    result: 'Полная защита кузова + 3 года гарантии на покрытие',
    services: ['Оклейка PPF полная', 'Антикор Dinitrol', 'Керамика на стёкла', 'Шумоизоляция арок'],
    price: '380 000 ₽',
    images: ['[PLACEHOLDER — процесс оклейки]'],
    proof: 'Акт + гарантийный талон',
  },
  {
    id: 4,
    category: 'Сервис и ТО',
    title: 'Li Auto L9 — ТО 15 000 км + диагностика',
    duration: '1 день',
    problem: 'Завод рекомендует ТО, но официального сервиса Li Auto в РФ нет',
    result: 'Выполнено полное ТО по регламенту завода',
    services: ['Замена масла', 'Диагностика высоковольтной батареи', 'Проверка ходовой', 'Обновление ПО'],
    price: 'от 45 000 ₽',
    images: ['[PLACEHOLDER — авто на подъёмнике]'],
    proof: 'Чек-лист + отчёт диагностики',
  },
  {
    id: 5,
    category: 'Импорт + растаможка',
    title: 'Voyah Free — импорт и регистрация за 12 дней',
    duration: '12 дней',
    problem: 'Клиент нашёл авто в Китае, нужна была помощь с логистикой',
    result: 'Авто доставлено, растаможено, зарегистрировано',
    services: ['Выкуп в Китае', 'Логистика', 'Таможня', 'Сертификация', 'Постановка на учёт'],
    price: '6 800 000 ₽',
    images: ['[PLACEHOLDER — авто в порту / на терминале]'],
    proof: 'ПТС + договор',
  },
  {
    id: 6,
    category: 'Продажа',
    title: 'NIO ES8 — продали за 2 недели по максимальной цене',
    duration: '14 дней',
    problem: 'Клиент не знал куда продать редкий китайский электрокар',
    result: 'Продано за 5,2 млн (на 300К выше рынка)',
    services: ['Предпродажная подготовка', 'Фотосессия', 'Размещение на площадках', 'Сопровождение сделки'],
    price: 'Комиссия 2,5%',
    images: ['[PLACEHOLDER — фото авто после подготовки]'],
    proof: 'Договор купли-продажи',
  },
  {
    id: 7,
    category: 'Гарантия',
    title: 'Dongfeng M-Hero 917 — гарантийный случай решён за 3 дня',
    duration: '3 дня',
    problem: 'Отказала мультимедиа, официальных сервисов в РФ нет',
    result: 'Заменён блок по гарантии, авто вернулось клиенту',
    services: ['Диагностика', 'Заказ блока из Китая', 'Замена', 'Проверка систем'],
    price: 'Бесплатно (гарантия)',
    images: ['[PLACEHOLDER — ремонт]'],
    proof: 'Гарантийный акт',
  },
  {
    id: 8,
    category: 'Улучшения',
    title: 'Zeekr 001 — тюнинг подвески + тонировка + доп. звук',
    duration: '4 дня',
    problem: 'Клиент хотел улучшить комфорт и внешний вид',
    result: 'Заниженная подвеска, атермальная тонировка, сабвуфер',
    services: ['Установка пневмоподвески', 'Атермальная тонировка', 'Сабвуфер + усилитель', 'Шумоизоляция'],
    price: '420 000 ₽',
    images: ['[PLACEHOLDER — авто до/после]'],
    proof: 'Фото + видео',
  },
]
```

**CTA в блоке:**
- "Получить расчёт по вашему кейсу" (основная кнопка)
- "Показать похожие кейсы" (фильтр)

---

### 5. **InStock** — Product cards (здесь карточки ОК)
**UI-паттерн:** Крупные карточки товара (как e-commerce)
- Карточки здесь уместны (это каталог)
- НО крупные, с большими фото, акцентом на цену и статус
- Desktop: 3 колонки, Mobile: 1 карточка на ширину экрана

**Улучшения:**
```tsx
// InStock.tsx — усилить как предложение
// Добавить:
// - Бейдж "Выгода 500К" если есть
// - Кнопка "Забронировать сейчас"
// - Таймер "Забронировано до XX.XX"
```

---

### 6. **HowWeWork** — Vertical timeline (NO CARDS)
**UI-паттерн:** Вертикальная timeline с шагами (объединить Lifecycle + Process)
- Без карточек
- Просто линия с точками + текст + иконка
- Минималистично

**Структура:**
```tsx
// components/HowWeWork.tsx (создать, заменить Lifecycle + Process)
<section className="section-padding bg-neutral-100">
  <div className="container-custom max-w-4xl">
    <h2 className="text-center mb-16">Как мы работаем</h2>
    
    <div className="relative">
      {/* Вертикальная линия */}
      <div className="absolute left-8 md:left-1/2 top-0 bottom-0 w-0.5 bg-neutral-300" />
      <motion.div 
        className="absolute left-8 md:left-1/2 top-0 w-0.5 bg-primary-500"
        style={{ height: progressHeight }}
      />

      {steps.map((step, i) => (
        <div className={`relative flex flex-col md:flex-row gap-8 mb-12 ${
          i % 2 === 0 ? '' : 'md:flex-row-reverse'
        }`}>
          
          {/* Точка на линии */}
          <div className="absolute left-8 md:left-1/2 w-4 h-4 rounded-full bg-primary-500 border-4 border-white -translate-x-1/2" />

          {/* Контент */}
          <div className={`flex-1 pl-20 md:pl-0 ${i % 2 === 0 ? 'md:text-right md:pr-12' : 'md:pl-12'}`}>
            <div className="inline-flex items-center gap-3 mb-3">
              <span className="w-12 h-12 rounded-full bg-primary-500/10 flex items-center justify-center">
                <Icon className="w-6 h-6 text-primary-500" />
              </span>
              <span className="text-4xl font-bold text-neutral-300">{String(i + 1).padStart(2, '0')}</span>
            </div>
            <h3 className="text-xl font-bold mb-2">{step.title}</h3>
            <p className="text-neutral-600">{step.description}</p>
            <div className="text-sm text-neutral-500 mt-2">{step.duration}</div>
          </div>

        </div>
      ))}
    </div>
  </div>
</section>
```

**Контент (6 шагов):**
```js
const steps = [
  {
    icon: MessageCircle,
    title: 'Консультация',
    description: 'Обсуждаем ваши пожелания, бюджет и сроки. Подбираем оптимальное решение.',
    duration: '30 минут',
  },
  {
    icon: FileSearch,
    title: 'Расчёт и договор',
    description: 'Фиксируем стоимость, сроки и список работ. Прозрачный договор без скрытых платежей.',
    duration: '1 день',
  },
  {
    icon: ShoppingCart,
    title: 'Поиск и выкуп авто',
    description: 'Находим ваш автомобиль в Китае, проверяем, выкупаем. Полное сопровождение сделки.',
    duration: '3–5 дней',
  },
  {
    icon: Plane,
    title: 'Логистика и таможня',
    description: 'Доставляем авто в Россию, оформляем все документы, растамаживаем.',
    duration: '7–10 дней',
  },
  {
    icon: Wrench,
    title: 'Подготовка под ключ',
    description: 'PPF, шумоизоляция, антикор, русификация, ТО — всё на нашем сервисе.',
    duration: '5–7 дней',
  },
  {
    icon: Key,
    title: 'Передача клиенту',
    description: 'Получаете готовый автомобиль с полным пакетом документов и гарантией.',
    duration: '1 день',
  },
]
```

---

### 7. **TrustBlock** — Icon grid + guarantees (NO SHADOWS)
**UI-паттерн:** Proof section без карточек
- Объединить WhyChoose + Guarantee
- Иконки + короткие тезисы в сетке (без рамок)
- Внизу — 4 главные гарантии (крупно, с иконками документов)

**Структура:**
```tsx
// components/TrustBlock.tsx (создать)
<section className="section-padding bg-white">
  <div className="container-custom">
    <h2 className="text-center mb-4">Почему нам доверяют</h2>
    <p className="text-center text-neutral-600 max-w-2xl mx-auto mb-12">
      7 лет работаем с китайскими автомобилями. Знаем все подводные камни импорта.
    </p>

    {/* Grid без карточек */}
    <div className="grid grid-cols-2 md:grid-cols-4 gap-8 mb-16">
      {reasons.map(reason => (
        <div className="text-center">
          <div className="w-16 h-16 mx-auto mb-4 rounded-full bg-primary-500/10 flex items-center justify-center">
            <Icon className="w-8 h-8 text-primary-500" />
          </div>
          <h3 className="font-bold mb-1">{reason.title}</h3>
          <p className="text-sm text-neutral-600">{reason.description}</p>
        </div>
      ))}
    </div>

    {/* Гарантии — крупно */}
    <div className="bg-neutral-50 rounded-2xl p-8 md:p-12">
      <h3 className="text-2xl font-bold mb-8 text-center">Гарантии</h3>
      <div className="grid md:grid-cols-2 gap-6">
        {guarantees.map(guarantee => (
          <div className="flex gap-4">
            <div className="w-12 h-12 flex-shrink-0 rounded-xl bg-primary-500 flex items-center justify-center">
              <Icon className="w-6 h-6 text-white" />
            </div>
            <div>
              <h4 className="font-bold mb-1">{guarantee.title}</h4>
              <p className="text-sm text-neutral-600">{guarantee.details}</p>
            </div>
          </div>
        ))}
      </div>
    </div>

    {/* CTA */}
    <div className="text-center mt-12">
      <button className="btn-secondary">Показать авто в наличии</button>
    </div>
  </div>
</section>
```

---

### 8. **FAQ** — Accordion (оставить как есть)
**UI-паттерн:** Accordion (уже реализован корректно)
- Без изменений (это правильный паттерн для FAQ)

---

### 9. **FinalCTA** — Full-width CTA с срочностью
**UI-паттерн:** Full-width блок с градиентом
- Без карточки
- Градиент фон
- Большой заголовок + 1 главная CTA
- Добавить триггер срочности

**Структура:**
```tsx
// FinalCTA.tsx — переделать
<section className="relative py-20 md:py-32 overflow-hidden">
  {/* Градиент фон */}
  <div className="absolute inset-0 bg-gradient-to-br from-neutral-900 via-neutral-800 to-neutral-900" />
  <div className="absolute inset-0 opacity-10">
    <Image src="/patterns/grid.svg" fill className="object-cover" />
  </div>

  <div className="container-custom relative z-10">
    <div className="max-w-3xl mx-auto text-center text-white">
      <motion.div
        initial={{ opacity: 0, y: 30 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
      >
        <h2 className="text-3xl md:text-5xl font-bold mb-6">
          Получите расчёт стоимости за 3 минуты
        </h2>
        <p className="text-lg md:text-xl text-neutral-300 mb-8">
          Ответьте на 5 вопросов — пришлём точный расчёт с учётом всех работ
        </p>

        {/* Триггер срочности */}
        <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary-500/20 border border-primary-500/30 text-primary-400 text-sm mb-8">
          <Clock className="w-4 h-4" />
          <span>Сегодня 3 слота свободно для консультации</span>
        </div>

        <button className="btn-primary-large">
          Рассчитать стоимость под ключ
        </button>

        <p className="text-sm text-neutral-400 mt-4">
          Расчёт бесплатный. Не передаём данные третьим лицам.
        </p>
      </motion.div>
    </div>
  </div>
</section>
```

---

## C) БЛОК "КЕЙСЫ" — ДЕТАЛЬНО

### Структура (см. пункт B4 выше)

### 6–8 кейсов (см. данные выше)

### Фильтры:
- Все кейсы
- Импорт из Китая
- Русификация
- Улучшения
- Сервис и ТО
- Гарантия
- Продажа авто

### Каждый кейс содержит:
1. **Категория** (бейдж)
2. **Заголовок** (модель + услуга)
3. **Срок** (реальный timeline)
4. **Проблема** (боль клиента)
5. **Результат** (решение + метрика)
6. **Что сделали** (список услуг)
7. **Стоимость** (цена под ключ)
8. **Доказательство** (фото/акт/видео)

### CTA:
- "Получить расчёт по вашему кейсу" (главная)
- Фильтры как доп. CTA ("Показать похожие кейсы")

### ⚠️ Места для реальных данных (что должен заполнить бизнес):
- [ ] Реальные фото всех кейсов (сейчас placeholder)
- [ ] Точные цены (сейчас примерные)
- [ ] Скриншоты актов/документов для блока "Доказательство"
- [ ] Before/After фото для русификации и защитных покрытий

---

## D) КОНКРЕТНЫЙ КОНТЕНТ (микрокопирайт)

### 1. Hero-заголовок + подзаголовок (2 варианта)

**Вариант 1 (про импорт):**
```
Заголовок: Привезём любой авто из Китая под ключ за 14 дней
Подзаголовок: Выкуп, логистика, таможня, подготовка, регистрация. Берём всю головную боль на себя — вы получаете готовый автомобиль.
```

**Вариант 2 (про экспертизу):**
```
Заголовок: Всё для вашего китайского авто — от импорта до гарантии
Подзаголовок: 7 лет привозим, обслуживаем и улучшаем автомобили из Китая. Единственный сервис полного цикла в Москве.
```

### 2. Буллеты "почему мы" (встроить в Hero или ProofBar)

Вместо отдельного блока — 4 floating badges в Hero:

```tsx
const heroBadges = [
  { icon: Shield, text: '7 лет на рынке' },
  { icon: Clock, text: 'От 14 дней под ключ' },
  { icon: Star, text: '420+ авто импортировано' },
  { icon: CheckCircle, text: 'Гарантия на всё' },
]
```

### 3. Тексты для кнопок CTA (3 варианта)

**Основная CTA (quiz):**
- Вариант 1: "Рассчитать стоимость под ключ" ✅ (рекомендуется)
- Вариант 2: "Узнать цену за 3 минуты"
- Вариант 3: "Получить расчёт моего кейса"

**Вторая CTA (InStock):**
- Вариант 1: "Показать авто в наличии" ✅
- Вариант 2: "Смотреть готовые авто"
- Вариант 3: "Купить авто сегодня"

**CTA после кейсов:**
- Вариант 1: "Сделать как в этом кейсе"
- Вариант 2: "Повторить этот кейс"
- Вариант 3: "Рассчитать по моему кейсу"

### 4. 6 коротких тезисов для trust/гарантии

```js
const trustPoints = [
  {
    icon: Shield,
    title: 'Юридическая чистота',
    description: 'Все авто с полным пакетом документов. ПТС, СБКТС, таможенные декларации.',
  },
  {
    icon: FileCheck,
    title: 'Гарантия 2 года',
    description: 'На все работы даём расширенную гарантию. Бесплатное устранение любых дефектов.',
  },
  {
    icon: Clock,
    title: 'Чёткие сроки',
    description: 'Фиксируем дедлайн в договоре. Если опоздали — компенсируем каждый день.',
  },
  {
    icon: Eye,
    title: 'Прозрачность 24/7',
    description: 'Видео с процесса работ, фото, чеки. Доступ к камерам сервиса онлайн.',
  },
  {
    icon: CreditCard,
    title: 'Оплата частями',
    description: 'Разбиваем платёж на этапы. Платите по факту выполнения каждого шага.',
  },
  {
    icon: Headphones,
    title: 'Поддержка после покупки',
    description: 'Бесплатные консультации весь срок владения. Помогаем с запчастями и ремонтом.',
  },
]
```

---

## E) DESIGN SYSTEM (конкретика)

### 1. Уровни фона секций (чередование)

```js
// Схема чередования
const sectionBackgrounds = {
  Hero: 'gradient (image overlay)',           // Fullscreen image
  ProofBar: 'bg-neutral-900',                 // Тёмный контраст
  Services: 'bg-neutral-100',                 // Светлый
  Cases: 'bg-white',                          // Чисто белый (акцент)
  InStock: 'bg-neutral-100',                  // Светлый
  HowWeWork: 'bg-neutral-100',                // Светлый
  TrustBlock: 'bg-white',                     // Белый
  FAQ: 'bg-neutral-100',                      // Светлый
  FinalCTA: 'bg-gradient (dark)',             // Тёмный градиент
  Contacts: 'bg-white',                       // Белый
  Footer: 'bg-neutral-900',                   // Тёмный
}
```

### 2. Тени, обводки, радиусы

**Правило: МЕНЬШЕ теней = ПРЕМИУМ**

```css
/* Убрать тени везде, кроме: */
.shadow-allowed {
  /* Только для карточек InStock (каталог) */
  /* Только для поп-апов/модалок */
}

/* Вместо теней — обводки */
.border-subtle {
  border: 1px solid theme('colors.neutral.200');
}

.border-strong {
  border: 2px solid theme('colors.neutral.900');
}

/* Радиусы */
.radius-sm { border-radius: 12px; }   /* Мелкие элементы */
.radius-md { border-radius: 16px; }   /* Карточки */
.radius-lg { border-radius: 24px; }   /* Секции */
.radius-xl { border-radius: 32px; }   /* Hero, крупные блоки */
```

### 3. Размерность: отступы, сетка

```css
/* Вертикальные отступы секций */
.section-padding {
  padding: 60px 0; /* mobile */
  @media (min-width: 768px) {
    padding: 100px 0; /* desktop */
  }
}

/* Горизонтальные отступы контейнера */
.container-custom {
  padding-left: 20px;
  padding-right: 20px;
  @media (min-width: 768px) {
    padding-left: 40px;
    padding-right: 40px;
  }
}

/* Сетка gap */
.grid-gap-sm { gap: 16px; }
.grid-gap-md { gap: 24px; }
.grid-gap-lg { gap: 32px; }
```

### 4. Использование акцентного цвета

**Правило: акцент только для важных элементов**

```js
// ✅ ГДЕ использовать #ad1a20 (красный)
const accentUsage = [
  'Кнопки CTA (primary)',
  'Цифры в ProofBar',
  'Активный фильтр в Cases',
  'Иконки в timeline (точки прогресса)',
  'Бейджи "Новое", "Выгода", "В наличии"',
  'Ховер на кнопки',
]

// ❌ ГДЕ НЕ использовать
const noAccent = [
  'Фоны секций (только белый/серый/чёрный)',
  'Все иконки подряд (только ключевые)',
  'Обводки карточек',
  'Текст (кроме цен)',
]
```

### 5. Премиум = меньше рамок, больше воздуха

**До (сейчас):**
```tsx
// ❌ Каждая карточка в рамке с тенью
<div className="bg-white border border-neutral-200 rounded-xl shadow-md p-6">
```

**После (премиум):**
```tsx
// ✅ Только типографика + воздух
<div className="py-8 border-b border-neutral-100 last:border-0">
  {/* Больше воздуха вокруг текста */}
</div>
```

**Типографика:**
```css
/* Увеличить размеры для премиальности */
h1, .text-display { font-size: 56px; line-height: 1.1; } /* desktop */
h2, .text-h1 { font-size: 40px; line-height: 1.2; }
h3, .text-h2 { font-size: 32px; line-height: 1.3; }

/* Больше letter-spacing для заголовков */
h1, h2 { letter-spacing: -0.02em; }

/* Увеличить line-height для body текста */
p { line-height: 1.7; }
```

**Фото:**
```tsx
// Крупные фото без рамок
<Image 
  src="..." 
  className="w-full aspect-[16/10] object-cover rounded-2xl"
  // НЕТ border, НЕТ shadow
/>
```

---

## F) ПЛАН ВНЕДРЕНИЯ (для фронта)

### Приоритет 1: За 1 день (критичные изменения)

**Задачи:**

1. **Создать ProofBar** (30 мин)
   - Новый компонент `components/ProofBar.tsx`
   - 4 статы: авто импортировано, лет на рынке, выполнено в 2025, повторных клиентов

2. **Переделать Services на split-screen** (2 часа)
   - `components/Services.tsx` — убрать карточки
   - Добавить активный state + image справа
   - Mobile: вертикальный stack

3. **Переместить Cases на 4-ю позицию** (5 мин)
   - В `HomeClient.tsx` поднять `<Cases />` после Services

4. **Удалить блоки** (10 мин)
   - Удалить `<OneCenter />` из HomeClient
   - Удалить `<ContactForm />` из середины (оставить только в конце)
   - Закомментировать `<WhyChoose />` и `<Guarantee />` (пока)

5. **Обновить FinalCTA** (30 мин)
   - Добавить триггер срочности
   - Градиент фон
   - Убрать карточку

**Итого: ~3.5 часа**

---

### Приоритет 2: За 3 дня (полный редизайн)

**День 1: Блок Cases (главный)**

1. **Создать фильтры кейсов** (1 час)
   - Табы сверху
   - State для активного фильтра
   - Логика фильтрации массива

2. **Переделать карточки кейсов** (3 часа)
   - Крупные карточки со слайдером фото
   - Детали: проблема, решение, результат, цена
   - Навигация по галерее
   - Hover-эффекты

3. **Добавить реальный контент** (1 час)
   - 8 кейсов из шаблона выше
   - Placeholder для фото (пометить где нужны реальные)

**День 2: HowWeWork + TrustBlock**

4. **Создать HowWeWork** (2 часа)
   - Новый компонент объединяющий Lifecycle + Process
   - Вертикальная timeline
   - 6 шагов работы
   - Анимация прогресса

5. **Создать TrustBlock** (2 часа)
   - Объединить WhyChoose + Guarantee
   - Сетка иконок без рамок
   - Блок гарантий внизу

6. **Удалить старые компоненты** (30 мин)
   - `components/Lifecycle.tsx` — удалить
   - `components/Process.tsx` — удалить
   - `components/WhyChoose.tsx` — удалить
   - `components/Guarantee.tsx` — удалить
   - `components/OneCenter.tsx` — удалить
   - `components/ContactForm.tsx` — удалить (или переместить в Contacts)

**День 3: Design system + полировка**

7. **Обновить globals.css** (1 час)
   - Новые размеры типографики
   - Убрать лишние тени
   - Обновить радиусы
   - Добавить utility классы

8. **Обновить Hero** (1 час)
   - Убрать карточки с features
   - Добавить floating badges
   - Новый заголовок (2 варианта A/B тест)

9. **Полировка InStock** (1 час)
   - Добавить бейджи "Выгода"
   - Кнопка "Забронировать"
   - Таймер статуса

10. **Тестирование + багфиксы** (2 часа)
    - Проверить все секции на mobile
    - Проверить анимации
    - Проверить фильтры кейсов
    - Исправить линты

---

### Компоненты для создания:

**Новые:**
- [ ] `components/ProofBar.tsx`
- [ ] `components/HowWeWork.tsx`
- [ ] `components/TrustBlock.tsx`
- [ ] `components/CaseCard.tsx` (опционально, если выносить карточку кейса)
- [ ] `components/CaseFilters.tsx` (опционально)

**Переписать:**
- [ ] `components/Services.tsx` (split-screen)
- [ ] `components/Cases.tsx` (полностью)
- [ ] `components/FinalCTA.tsx` (градиент + срочность)
- [ ] `components/Hero.tsx` (убрать карточки)
- [ ] `components/InStock.tsx` (добавить бейджи)

**Удалить:**
- [ ] `components/Lifecycle.tsx`
- [ ] `components/Process.tsx`
- [ ] `components/WhyChoose.tsx`
- [ ] `components/Guarantee.tsx`
- [ ] `components/OneCenter.tsx`
- [ ] `components/ContactForm.tsx` (если не нужна в середине)

---

## ИТОГОВЫЙ ЧЕКЛИСТ

### Структура ✅
- [x] План нового флоу (9 блоков вместо 13)
- [x] Обоснование удалений/объединений
- [x] Определены 2 главные CTA

### Anti-card стратегия ✅
- [x] Hero: fullscreen immersive
- [x] ProofBar: horizontal strip
- [x] Services: split-screen
- [x] Cases: gallery slider
- [x] InStock: product cards (уместны)
- [x] HowWeWork: vertical timeline
- [x] TrustBlock: icon grid без рамок
- [x] FAQ: accordion
- [x] FinalCTA: full-width с градиентом

### Блок Cases ✅
- [x] Структура секции
- [x] 8 кейсовых карточек с шаблонами
- [x] Фильтры (7 категорий)
- [x] 2 CTA в блоке
- [x] Помечены места для реальных данных

### Контент ✅
- [x] 2 варианта hero-заголовка
- [x] 4 буллета для hero badges
- [x] 3 варианта CTA текстов
- [x] 6 тезисов trust/гарантии

### Design system ✅
- [x] Схема чередования фонов
- [x] Правила теней/обводок/радиусов
- [x] Размерность (padding, gap)
- [x] Стратегия использования акцента
- [x] Принципы премиальности

### План внедрения ✅
- [x] Список задач на 1 день (критично)
- [x] Список задач на 3 дня (полный редизайн)
- [x] Компоненты: создать/переписать/удалить
- [x] Приоритизация

---

## 🎯 КЛЮЧЕВЫЕ МЕТРИКИ УСПЕХА

После редизайна ожидаем:

1. **Снижение Bounce Rate** на 20–30%
   - Из-за разнообразия секций (не скучно)

2. **Рост времени на сайте** на 40–50%
   - Благодаря блоку Cases (люди будут листать кейсы)

3. **Рост конверсии в квиз** на 25–35%
   - CTA в 3 местах + триггер срочности

4. **Рост trust-показателей**
   - Cases + Proof + Guarantees = мощная связка

---

**Готово! План на 100% детальный и готов к внедрению.**

Начинаем с Приоритета 1? 🚀
